# Auth0 (required for login)
# In Auth0 Dashboard:
# 1. Add app URLs to Allowed Callback/Logout URLs and Web Origins (e.g. http://localhost:5173)
# 2. Create an API (Applications → APIs → Create API) with Identifier = AUTH0_AUDIENCE below
#    (needed for backend to accept tokens; 401 = API missing or identifier mismatch)
VITE_AUTH0_DOMAIN=
VITE_AUTH0_CLIENT_ID=
VITE_AUTH0_AUDIENCE=totally-unique-backend-id

# API base URL (optional)
# Defaults to http://localhost:3000 in dev, and app code appends /api automatically.
# You can set either the server origin or a full /api URL.
# VITE_API_URL=https://cultivate-be.vercel.app
